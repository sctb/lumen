#!/bin/sh

DIR=`pwd`
cd "`dirname $0`"
HOME=`pwd`
cd $DIR

HOST=${LUMEN_HOST:-lua}
CODE=lumen.lua

if [ "$HOST" = "node" ]
then
    CODE=lumen.js
    RUNTIME=$HOME/../obj/runtime.js
fi

exec $HOST $HOME/$CODE $RUNTIME "$@"
