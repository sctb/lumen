#!/bin/sh

DIR=`pwd`
cd "`dirname $0`"
HOME=`pwd`
cd $DIR

HOST=${LUMEN_HOST:-lua}

case $HOST in
    node*)
        CODE=lumen.js
        export NODE_PATH=$HOME:$DIR/lib;;
    *)
        CODE=lumen.lua
        export LUA_PATH="$HOME/?.lua;$DIR/lib/?.lua;;";;
esac

exec $HOST $HOME/$CODE "$@"
